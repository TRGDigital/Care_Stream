// The marketing site's header is the theme's (site-header.tsx). This name is kept because the
// marketing layout and the home page import it; both now render the theme header and mega menus.
export { SiteHeader as MarketingNav } from './site-header'
