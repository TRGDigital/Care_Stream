{
  "version": 2,
  "builds": [
    {
      "src": "src/app.ts",
      "use": "@vercel/node",
      "config": { "maxDuration": 300, "memory": 1769 }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/src/app.ts" }
  ],
  "_crons": "Scheduling moved to Postgres (pg_cron) in prisma/migrations/manual_cron_schedules.sql. These Vercel crons stopped silently on 5 June 2026 and went unnoticed for 96 days. Listing them here as well would double-fire every job if Vercel's scheduler ever resumes."
}
