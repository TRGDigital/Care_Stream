// §4.1.1 — Generate vector embeddings via text-embedding-3-small (OpenAI)
// Same model used for both ingestion and query phases (§4.1.2)
export async function embedTexts(_texts: string[]): Promise<number[][]> {
  throw new Error('Not implemented')
}
