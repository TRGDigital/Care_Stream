// Typed API client wrapping fetch calls to the Express API
// Attaches Bearer token from session on every request
// Response envelope: { success, data, meta } | { success, error } (§12.2)
export const apiClient = {} as any // placeholder until feature build
